const config = require(`./docusaurus.core.config.js`);

config.baseUrl = `/stale/`;
config.deploymentBranch = `gh-pages`;
config.url = `https://sonia-corporation.github.io`;

module.exports = config;
